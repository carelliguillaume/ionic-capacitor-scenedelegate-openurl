import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vinci.autoroutes.ulys.alpha',
  appName: 'IonicCapacitorSceneDelegate',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
